var React = require('react');

var Header = React.createClass({
  render: function () {
    return (
      <header className="col-md-12">
          <h2>ReactMovie</h2>
      </header>
    );
  }
});

module.exports = Header;
